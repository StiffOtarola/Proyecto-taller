[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --prefix backend",
  "npm install --prefix frontend"
]

[phases.build]
cmds = [
  "cd frontend && npx ng build --configuration=production"
]

[start]
cmd = "NODE_ENV=production node backend/src/server.js"
